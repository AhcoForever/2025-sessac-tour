# 공모전 보고서 - 적용기술 및 서비스 방법

## 📌 적용기술

### 개발 플랫폼 및 백엔드 인프라
본 프로젝트는 Flutter 3.9.0 기반의 크로스 플랫폼 애플리케이션으로, 단일 코드베이스로 Android, iOS, Web을 동시 지원합니다. 백엔드는 Firebase를 활용한 서버리스 아키텍처로 구축하여 별도의 서버 운영 없이 Authentication(사용자 인증), Firestore(데이터베이스), Storage(파일 저장)를 통합 운영하고 있습니다.

### AI 가이드 시스템
핵심 차별화 요소인 캐릭터 기반 AI 가이드는 Claude AI의 실시간 스트리밍 기능을 활용하여 구현했습니다. 세 가지 서울 캐릭터(소울해치, 댕댕청룡, 돌격백호)별 고유 페르소나를 시스템 프롬프트에 주입하고, RAG(Retrieval-Augmented Generation) 기법으로 서울 공공데이터를 실시간 컨텍스트에 통합하여 정확하고 개성 있는 관광 정보를 제공합니다.

### 지도·위치 기반 서비스 및 공공데이터 연동
Google Maps Flutter SDK와 Geolocator를 활용하여 실시간 GPS 추적과 관광지 마커 표시 기능을 구현했으며, 서울관광재단의 VisitSeoul API와 서울시 열린데이터광장 API를 연동하여 관광명소, 문화행사, 야경명소 등의 최신 정보를 4개 언어(한/영/일/중)로 제공합니다.

### 기술 스택 요약

| 분류 | 기술 | 적용 목적 |
|------|------|----------|
| **프론트엔드** | Flutter 3.9.0, Dart | 크로스 플랫폼 개발, 고성능 UI 렌더링 |
| **백엔드** | Firebase (Auth, Firestore, Storage) | 서버리스 인증, 데이터베이스, 파일 저장 |
| **AI** | Claude AI (Anthropic) | 실시간 스트리밍 대화, 캐릭터 페르소나 구현 |
| **지도/위치** | Google Maps, Geolocator | 실시간 위치 추적, 관광지 마커 표시 |
| **공공 API** | VisitSeoul API, 서울 열린데이터 API | 관광 정보, 문화 행사 데이터 연동 |
| **상태 관리** | Provider | 효율적인 전역 상태 관리 |
| **보안** | Flutter Dotenv | API 키 환경 변수 분리 관리 |

전체 프로젝트는 클린 아키텍처 패턴에 따라 Presentation, Business Logic, Service, Data 레이어로 분리되어 있으며, 8개의 독립 모듈(auth, ai, map, public_data, camera, setting, meet_up, error)로 구성하여 유지보수성과 확장성을 확보했습니다.

---

## 🔧 서비스 방법

본 애플리케이션은 사용자가 앱을 최초 실행하면 3초간의 스플래시 화면을 통해 Firebase 초기화와 환경 변수 로딩을 수행한 후, 서울 광화문을 중심으로 한 지도 화면을 기본 시작 화면으로 제공합니다. 이 과정에서 위치 권한을 요청하며, 사용자가 허용할 경우 실시간 GPS를 활성화하여 현재 위치를 지도 중심으로 자동 이동시킵니다.

사용자 인증 서비스는 Firebase Authentication 기반으로 동작하며, 회원가입 시 이메일과 비밀번호를 입력받고 전화번호를 자동 포맷팅(010-xxxx-xxxx)하여 저장합니다. 가입이 완료되면 Cloud Firestore의 `users` 컬렉션에 사용자 고유 ID(uid), 이메일, 전화번호, 가입일(createdAt), 마지막 로그인 시간(lastLoginAt) 등의 프로필 정보가 저장되며, Firebase 보안 규칙을 통해 각 사용자는 자신의 데이터만 접근할 수 있도록 제한됩니다. 로그인 세션은 SharedPreferences를 활용하여 로컬에 유지되며, 비밀번호 재설정은 이메일 인증 링크를 통해 처리됩니다. 계정 삭제는 보안을 위해 반드시 재인증을 거친 후에만 실행되도록 설계되어 있습니다.

AI 캐릭터 가이드 시스템은 사용자가 세 가지 캐릭터 중 하나를 선택하면, 해당 캐릭터의 성격과 말투가 시스템 프롬프트에 주입되어 Claude AI와의 대화가 시작됩니다. PromptBuilder 클래스가 동적으로 프롬프트를 생성하는데, 이때 현재 시간, 날씨, 사용자 위치 정보뿐만 아니라 VisitSeoul API와 서울 열린데이터광장 API에서 실시간으로 가져온 문화 행사, 공원 정보, 야경 명소 데이터를 함께 컨텍스트로 제공합니다. 사용자가 메시지를 입력하면 Claude API의 스트리밍 엔드포인트로 POST 요청이 전송되고, Server-Sent Events(SSE) 방식으로 응답이 실시간으로 수신되어 화면에 한 글자씩 출력됩니다. AI는 항상 3가지 카테고리(힐링형, 활력형, 문화형)로 분류된 추천을 제공하며, 각 추천에는 장소명, 설명, 이유가 포함된 구조화된 JSON 형식이 적용됩니다.

지도 기반 관광지 탐색 서비스는 앱 실행과 동시에 LocationService를 통해 GPS 권한을 확인하고 사용자의 현재 위치를 획득합니다. 이후 VisitSeoulApiService와 SeoulApiService가 비동기적으로 관광지, 야경 명소 데이터를 API로부터 받아와 MarkerService가 이를 Google Maps에 표시할 마커로 변환합니다. 사용자가 지도를 확대하거나 축소하면 줌 레벨 변화를 감지하여 마커 아이콘의 크기가 동적으로 조정되며, 마커를 터치하면 Custom Info Window가 해당 위치 위에 말풍선 형태로 나타나 간단한 정보를 표시합니다. 마커를 다시 터치하면 화면 하단에서 Bottom Sheet가 올라오며, 여기에는 관광지 이름, 주소, 카테고리, 고해상도 이미지, HTML 형식의 상세 설명, 운영 시간, 연락처 등이 풍부하게 제공됩니다. DestinationService는 사용자 위치와 목적지 간의 거리를 실시간으로 계산하여 일정 거리 내에 도착하면 알림을 발생시키며, 사용자는 이 지점에서 카메라 촬영 화면으로 바로 이동할 수 있습니다.

공공 데이터 탐색 기능은 사용자가 화면 상단의 카테고리(문화행사, 관광명소, 공원, 야경, 문화공간)를 선택하면, 해당 API 엔드포인트로 HTTP GET 또는 POST 요청을 전송하여 데이터를 받아옵니다. VisitSeoul API의 경우 페이지 번호, 정렬 방식(최신순/가나다순), 언어 코드를 파라미터로 전달하며, 응답으로 받은 JSON 데이터는 각 모델 클래스(TourContent, CulturalEvent, ParkInfo 등)로 역직렬화됩니다. 리스트 화면에서는 페이지네이션을 구현하여 사용자가 스크롤을 내리면 자동으로 다음 페이지 데이터를 로딩하며, 이미지는 레이지 로딩 방식으로 필요한 시점에만 다운로드되어 네트워크 부하를 줄입니다. 각 항목을 터치하면 DetailPage로 라우팅되며, Flutter HTML 패키지를 사용하여 HTML 태그가 포함된 설명을 웹뷰 없이도 네이티브하게 렌더링합니다.

사진 메모리 기능은 사용자가 카메라 버튼을 누르면 Permission Handler가 먼저 카메라 권한을 확인하고, 권한이 있을 경우 Camera 패키지를 통해 카메라 프리뷰를 활성화합니다. 사용자가 셔터 버튼을 누르면 촬영된 이미지가 임시 파일로 저장되고, 동시에 Geolocator를 통해 현재 GPS 좌표를 획득하여 이미지 메타데이터에 위치 정보를 태깅합니다. 이후 Firebase Storage의 `photo_memories` 경로에 사용자별 폴더 구조로 업로드되며, 업로드가 완료되면 다운로드 URL이 반환되어 Cloud Firestore에 참조 정보로 저장됩니다. 갤러리 화면에서는 사용자가 촬영한 모든 사진을 Grid 형태로 표시하며, 각 사진을 터치하면 촬영 일시, 위치, 전체 해상도 이미지를 확인할 수 있습니다.

상태 관리는 Provider 패키지를 통해 전역적으로 처리됩니다. 예를 들어 AuthProvider는 현재 로그인 상태와 사용자 정보를 관리하고, ChatProvider는 AI와의 대화 기록과 현재 선택된 캐릭터 정보를 유지하며, MapProvider는 지도의 현재 중심 좌표, 줌 레벨, 선택된 마커 상태를 관리합니다. 이러한 Provider들은 ChangeNotifier를 상속받아 데이터가 변경될 때마다 notifyListeners()를 호출하여 해당 데이터를 구독하는 위젯들만 선택적으로 리빌드되도록 최적화되어 있습니다.

화면 전환 및 내비게이션은 GoRouter가 담당하며, 앱의 모든 라우트는 app_router.dart에서 중앙 집중식으로 정의됩니다. 각 화면은 고유한 경로를 가지며(예: `/login`, `/map`, `/ai-chat/:characterId`), 파라미터를 URL에 포함시켜 전달할 수 있습니다. 딥링크 기능을 통해 외부 앱이나 웹 브라우저에서 특정 링크(예: `sessactour://map?place=123`)를 클릭하면 앱이 실행되면서 해당 화면으로 직접 이동하며, 브라우저의 뒤로 가기 버튼처럼 자연스러운 네비게이션 스택 관리가 가능합니다.

보안 및 에러 처리는 다층적으로 구현되어 있습니다. 모든 API 키는 .env 파일에 저장되어 코드 저장소에 커밋되지 않으며, Firebase 보안 규칙은 Firestore와 Storage 레벨에서 사용자별 접근 권한을 제한합니다. 네트워크 요청 실패 시에는 한국어로 번역된 에러 메시지가 표시되며, Firebase Authentication의 영문 에러 코드(예: `user-not-found`)는 AuthService 내부에서 "사용자를 찾을 수 없습니다"와 같은 친절한 한국어 메시지로 변환됩니다. 위치 권한이 거부된 경우에는 설정 화면으로 이동할 수 있는 안내 다이얼로그가 표시되며, API 호출 중 네트워크 오류가 발생하면 재시도 옵션을 제공합니다.

성능 최적화를 위해 여러 전략이 적용되었습니다. 이미지는 Flutter의 CachedNetworkImage를 활용하여 한 번 다운로드된 이미지는 로컬 캐시에 저장되어 재요청 시 즉시 표시되며, 리스트 화면에서는 ListView.builder를 사용하여 화면에 보이는 항목만 렌더링하는 가상 스크롤링을 구현했습니다. API 응답은 SharedPreferences에 일정 시간 캐싱되어 동일한 요청을 반복할 때 서버 부하를 줄이고, JSON 파싱은 Isolate를 활용하여 백그라운드 스레드에서 처리되어 UI 스레드의 블로킹을 방지합니다.

전체적인 데이터 흐름은 사용자가 UI 위젯과 상호작용하면 해당 이벤트가 Provider로 전달되고, Provider는 적절한 Service 클래스의 메서드를 호출하여 비즈니스 로직을 실행합니다. Service 계층에서는 Firebase SDK나 HTTP 클라이언트를 통해 외부 API와 통신하며, 받은 데이터는 Model 클래스로 변환되어 Provider에 저장됩니다. Provider의 데이터가 변경되면 해당 데이터를 구독하는 UI 위젯들이 자동으로 재렌더링되어 최신 상태를 반영하는 단방향 데이터 흐름 아키텍처가 구축되어 있습니다.
